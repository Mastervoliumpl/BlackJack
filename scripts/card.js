export class Card {
    constructor(suit, value, numericValue, imagePath) {
        this.suit = suit;
        this.value = value;
        this.numericValue = numericValue;
        this.imagePath = imagePath;
    }
}